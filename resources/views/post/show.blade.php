<!DOCTYPE html>
<html>
<head>
    <title>title</title>
</head>
<body>
    <!-- Работает -->
    <!-- @foreach ($arr as $key=>$elem)
        {{$key}} {{$elem}}
    @endforeach -->


    <!-- @foreach ($arr as $key=>$elem)
        {{$key+1}}{{$elem}}
    @endforeach -->
</body>
</html>